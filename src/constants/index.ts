/**
 * HYDRE V1.0 — Constants Barrel Export
 * V4.0.0-HYDRE-APEX Compliant
 */

export {
    EASING,
    DURATION,
    DURATION_CSS,
    ANIMATION_PRESET,
    STAGGER,
    KEYFRAMES,
    createTransition,
    getAllKeyframes,
} from './animation';

export type { EasingKey, DurationKey } from './animation';
